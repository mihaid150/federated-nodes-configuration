apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fog2-ws
  namespace: fog2
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - host: fog2.local.lan
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: fog-app
            port:
              number: 8081

